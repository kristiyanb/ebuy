@model EBuy.Web.Models.Categories.CategoryViewModel

@{
    ViewData["Title"] = Model.Name;
}

<h1>@Model.Name</h1>
<div class="d-flex justify-content-between">
    <div class="text-black-50">
        @Model.Products.Count Products
    </div>
    <div class="dropdown show">
        <a class="btn text-dark dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Sort By
        </a>

        <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
            <a class="dropdown-item" asp-controller="Products" asp-action="Category" asp-route-orderBy="Name">Name</a>
            <a class="dropdown-item" asp-controller="Products" asp-action="Category" asp-route-orderBy="Rating">Rating</a>
            <a class="dropdown-item" asp-controller="Products" asp-action="Category" asp-route-orderBy="Price">Price Asc.</a>
            <a class="dropdown-item" asp-controller="Products" asp-action="Category" asp-route-orderBy="PriceDescending">Price Desc.</a>
        </div>
    </div>
</div>

<hr class="mb-5" />

<div class="d-flex flex-md-wrap">
    @Html.DisplayFor(x => x.Products)
</div>